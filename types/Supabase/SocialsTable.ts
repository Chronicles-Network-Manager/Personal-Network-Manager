export default interface SocialsTable {
    id: string;
    createdAt: string; // ISO date string
    updatedAt: string; // ISO date string
    instagram: string;
    linkedin: string;
    discord: string;
    reddit: string;
    github: string;
    other: string;
    userId: string;
}